# 虛構設備異常紀錄整理器 v0.1

以虛構設備紀錄示範資料驗證、狀態分類與摘要輸出的 Python 小作品。

版本狀態：v0.1 展示版，已完成固定六筆虛構資料基準驗收。

## 功能

- - 檢查紀錄是否包含 `id` 與 `status`，並檢查 `id` 是否為空字串 `""`。
- 將有效紀錄分為 `pending`、`processing`、`completed`。
- - 將缺少必要欄位、`id` 為空字串或狀態不在分類規則內的紀錄保留在未知分類，顯示對應原因。
- 顯示總紀錄數、各分類數量與完成率。
- 依序提示：檢查未知狀態 → 追蹤處理中紀錄 → 開始處理待處理紀錄；若皆無則顯示目前沒有待處理工作。

完成率以「已完成數量 ÷ 總紀錄數 × 100」計算，總紀錄數包含未知分類，顯示至小數點後一位。
- 列出待處理（pending）紀錄的 id，方便查看需要處理的項目。

## 檔案與資料

| 檔案 | 用途 |
| --- | --- |
| `alarm_record_tool_v2.py` | 主程式、記憶體內範例資料與手動驗收函式 |
| `README.md` | 使用方式、基準結果與版本限制 |
| `demo_result.jpg` | 程式執行結果展示圖 |

範例資料直接保存在主程式的 `records` 中。程式不讀取外部 JSON；正常執行與驗收均在同一個 Python 檔案內完成。

## 正常執行

使用 Python 3。手機操作方式：在 Pydroid 3 開啟並儲存 `alarm_record_tool_v2.py`，按下執行鍵。

檔案最底下的預設入口應為：

```python
if __name__ == "__main__":
    main()
```

`main()` 負責整理與顯示資料，不呼叫固定六筆資料的驗收函式。

## 手動驗收

`run_v01_test()` 會將目前的 `records` 分類、建立摘要，再交由 `validate_v01()` 核對六項基準。

1. 暫時將檔案最底下的入口改為下方程式碼。
2. 儲存並以一般 Python 模式執行；驗收使用 `assert`，不使用 `-O` 最佳化模式。
3. 確認顯示 `v0.1 基準驗收：PASS`。
4. 驗收後將最底下的呼叫改回 `main()`，並儲存。

```python
if __name__ == "__main__":
    run_v01_test()
```

固定範例的預期結果：

| 驗收項目 | 預期值 |
| --- | --- |
| 總紀錄數 | 6 |
| 待處理數量 | 1 |
| 處理中數量 | 1 |
| 已完成數量 | 1 |
| 未知狀態數量 | 3 |
| 完成率 | `"16.7%"` |

三筆未知紀錄分別涵蓋：狀態值不在分類規則內、缺少 `status`、缺少 `id`。

任一基準不符會觸發 `AssertionError`。這是固定範例的回歸驗收；若主動更改 `records`，應重新確認預期結果。正常資料處理不要求紀錄必須剛好六筆。

## 已驗證範圍與限制

2026/09/05 使用者在 Pydroid 3 執行回報：單檔基準驗收 PASS，預設入口恢復 `main()` 後正常啟動 PASS。六筆範例的分類、完成率、未知原因與處理提示已完成手機操作驗證。

本版僅使用虛構資料及終端文字輸出，不包含 Database、Web、API、SaaS 或外部資料檔讀取。空資料集及其他未列出的輸入組合，尚未列入本次完成的驗收結果。

細節驗收（2026/09/12 補充）

新增手動驗收函式 "run_detail_test()"，使用原本六筆虛構資料，核對：

- 三筆未知紀錄的原因及順序：「狀態值不在分類規則內」、「缺少status」、「缺少id」。
- 有未知紀錄時，優先提示為「優先處理：檢查未知狀態」。

執行方式：暫時將檔案底部入口中的 "main()" 改為 "run_detail_test()"，儲存並以一般 Python 模式執行，不使用 "-O"。通過時顯示「未知原因與處理提示驗收：PASS」。驗收後恢復 "main()" 並儲存。

驗證紀錄：細節驗收 PASS；故意改錯一項預期原因時，成功觸發 "AssertionError"；恢復正確內容後，使用者確認再次 PASS，並恢復正常入口。

本次未涵蓋其他優先提示分支，也不代表所有輸入組合均已驗證。

## 空字串檢查整合驗證（2026/09/20 補充）

在 `validate_record()` 中加入 `id` 空字串檢查，順序為：缺少 `id` → 缺少 `status` → `id` 為空字串。遇到第一個不符合條件的項目即回傳原因。

使用正常入口 `main()`，暫時將原六筆資料中第一筆已完成紀錄的 `id` 改為 `""`，確認：

- 該筆紀錄保留在未知分類，原因為「id不可為空字串」。
- 總數 6／待處理 1／處理中 1／已完成 0／未知 4／完成率 0.0%。

恢復第一筆 `id` 為 `EQ-001` 後，使用者回報結果正常：總數 6／待處理 1／處理中 1／已完成 1／未知 3／完成率 16.7%。

本次為正常入口的手動整合驗證，未新增自動驗收函式。檢查僅針對空字串 `""`；純空白字串、其他型別及其他未列出的輸入組合，不在本次驗證範圍。

## 待處理明細顯示驗證（2026/09/26 補充）

新增 `display_pending(pending)`，由 `main()` 呼叫，依序列出待處理紀錄的 id。

已在 Pydroid 3 以正常入口手動確認：

- 原六筆資料：顯示待處理紀錄 `EQ-002`，統計維持原基準。
- 暫將第一筆狀態改為 `pending`：依序顯示 `EQ-001`、`EQ-002`；總數 6／待處理 2／處理中 1／已完成 0／未知 3／完成率 0.0%。

兩次執行均正常結束。驗證後，使用者確認已將第一筆恢復為 `completed` 並儲存。

本次僅確認上述一筆及兩筆待處理案例，未驗證零筆待處理情境，未新增自動驗收函式。
